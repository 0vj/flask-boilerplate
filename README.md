# flask-boilerplate
my boilerplate for flask projects 



``` 
$ python -m venv venv
$ . venv/bin/activate
$ pip install -r requirements.txt
$ flask run
```

database:

```
$ flask db init
$ flask db migrate
$ flask db upgrade
```
